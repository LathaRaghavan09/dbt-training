version: 2

sources:
  - name: globalmart
    database: raw
    schema: globalmart
    tables:
      - name: customer
        columns:
          - name: customerid
            tests:
              - not_null
              - unique
      - name: orders
        columns:
          - name: orderid
            tests:
              - unique
      - name: product

